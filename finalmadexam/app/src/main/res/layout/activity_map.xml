<?xml version="1.0" encoding="utf-8"?>
<!--
    STEP 5 (cont.) — Map Activity Layout
    Renamed from activity_main.xml (com.fam.favlocation) → activity_map.xml.
    Shows the site title, a Google Map, a back button, and a camera button (STEP 4).
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/forest_deep">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/forest_mid"
        android:paddingTop="28dp"
        android:paddingBottom="10dp"
        android:paddingHorizontal="20dp">

        <!-- Back button -->
        <LinearLayout
            android:id="@+id/btn_back_map"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingVertical="2dp"
            android:layout_marginBottom="6dp"
            android:clickable="true"
            android:focusable="true">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="←"
                android:textColor="@color/brass_light"
                android:textSize="14sp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="  ALL SITES"
                android:textColor="@color/brass_light"
                android:textSize="9sp"
                android:letterSpacing="0.25"
                android:fontFamily="serif"/>
        </LinearLayout>

        <!-- Site title -->
        <TextView
            android:id="@+id/tv_map_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Heritage Site"
            android:textColor="@color/ivory"
            android:textSize="15sp"
            android:textStyle="bold"
            android:fontFamily="serif"
            android:maxLines="2"
            android:ellipsize="end"
            android:lineSpacingMultiplier="1.2"
            android:layout_marginBottom="8dp"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/divider_brass"/>
    </LinearLayout>

    <!-- Google Map -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/bg_map_rounded">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="2dp"
            app:cardCornerRadius="15dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/forest_deep">

            <fragment
                android:id="@+id/id_map"
                android:name="com.google.android.gms.maps.SupportMapFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

        </androidx.cardview.widget.CardView>
    </FrameLayout>

    <!-- Bottom bar: description + camera button (STEP 4) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp">

        <TextView
            android:id="@+id/tv_map_desc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cavite Cultural Heritage Site"
            android:textColor="@color/text_moss"
            android:textSize="10sp"
            android:fontFamily="serif"
            android:lineSpacingMultiplier="1.3"
            android:paddingEnd="12dp"/>

        <!-- Camera button (STEP 4) -->
        <LinearLayout
            android:id="@+id/btn_camera"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:background="@drawable/bg_button_camera"
            android:clickable="true"
            android:focusable="true">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📷"
                android:textSize="13sp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="  CAPTURE"
                android:textColor="@color/forest_deep"
                android:textSize="9sp"
                android:letterSpacing="0.15"
                android:textStyle="bold"
                android:fontFamily="serif"/>
        </LinearLayout>
    </LinearLayout>

</LinearLayout>